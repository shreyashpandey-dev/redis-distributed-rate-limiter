spring:
  application:
    name: ratelimiter
  data:
    redis:
      # Spring Boot 4: spring.data.redis.* (env: SPRING_DATA_REDIS_HOST / SPRING_DATA_REDIS_PORT)
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}